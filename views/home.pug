extends ./layouts/main
include ./mixins/videoBlock

block content
    .home-videos
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
        each items in video
            +VideoBlock({
                id : items.id,
                videoFile : items.fileUrl,
                title : items.title,
                views : items.views,
                description : items.description
            })
            
        
